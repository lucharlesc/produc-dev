import * as clu from "../clu.js";

export default class HeaderComponent extends clu.Component {
    styles = ``;
    state = {};
    events = {};
    render() {
        return `<p>header</p>`;
    }
}